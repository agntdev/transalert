# TransAlert

Private Telegram bot for a single owner that imports bank transactions, auto-categorizes them (editable), stores encrypted credentials, and sends instant DM alerts for every transaction.

Spec: [`docs/blueprint.md`](docs/blueprint.md).

Built on [agnt-gm.ai](https://agnt-gm.ai). The whole bot is built and refined here as pull requests across successive build passes.
